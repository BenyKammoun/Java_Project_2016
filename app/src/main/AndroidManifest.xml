<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.a2016.proj.bensimhon.kamoun.java_project_2016">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".controller.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name=".model.backend.BusinessActivityContentProvider"
            android:authorities="com.a2016.proj.BusinessActivity"
            android:enabled="true"
            android:exported="true" />

        <provider
            android:name=".model.backend.BusinessContentProvider"
            android:authorities="com.a2016.proj.Business"
            android:enabled="true"
            android:exported="true" />
    </application>

</manifest>